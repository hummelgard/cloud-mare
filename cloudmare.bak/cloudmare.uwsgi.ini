[uwsgi]
# the python module to import
chdir = /srv/http/cloudmare/

wsgi-file = cloudmare/wsgi.py
#module = cloudmare.wsgi.application
# the variable the holds the flask context
#callable = cloudmare 
plugin = python
Processes = 10
master = True 
socket = %drun/%n.sock
#chmod-socket    = 666
#pidfile = %drun/.pid
logto = %dlog/%n.log
uid = http
gid = http
vacuum = true
